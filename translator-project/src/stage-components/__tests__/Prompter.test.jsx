import { render, screen } from '@testing-library/react';
import PrompterComponent from '../PrompterComponent';
import { vi } from "vitest";


test('renders words with spans and break lines', () => {
  // mock the props for the component
  // const data = ['Hello', 'world', 'this', 'is', 'a', 'test'];
  // const setCompleted = vi.fn();
  // const setObjectData = vi.fn();
  // const objectData = { current: [] };

  // // render the component with the props
  // render(
  //   <PrompterComponent
  //     data={data}
  //     setCompleted={setCompleted}
  //     setObjectData={setObjectData}
  //     objectData={objectData}
  //   />
  // );

  // // check if the words are rendered with spans
  // data.forEach((word) => {
  //   expect(screen.getByText(word)).toBeInTheDocument();
  // });
});