import { render, screen } from '@testing-library/react';
import SpanComponent from '../SpanComponent';

test('should render the span with the given text', () => {
  // render(
  //   <SpanComponent
  //     text="Helloooooooooooooooooooo"
  //     handleSpanInfo={(width) => {if(width > 900){return true}else {return false}}}
  //     id={0}
  //     wordsObjectArray={{ current: [] }}
  //   />
  // );
  // const spanElement = screen.getByRole("span")
  // console.log(spanElement.children[0].innerHTML);
  // expect(spanElement).toBeInTheDocument();
  
});